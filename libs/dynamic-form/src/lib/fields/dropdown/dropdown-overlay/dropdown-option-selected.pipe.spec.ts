import { DropdownOptionSelectedPipe } from './dropdown-option-selected.pipe';

describe('DropdownOptionSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownOptionSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
