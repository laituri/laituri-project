import { DropdownInputValuePipe } from './dropdown-input-value.pipe';

describe('DropdownInputValuePipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownInputValuePipe();
    expect(pipe).toBeTruthy();
  });
});
