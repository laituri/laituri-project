import { DynamicFormFieldComponentConfig } from '../../dynamic-form.types';
import { SubmitComponent } from './submit.component';

export const SubmitComponentConfig: DynamicFormFieldComponentConfig = {
  key: 'submit',
  component: SubmitComponent,
  type: 'visual',
};
