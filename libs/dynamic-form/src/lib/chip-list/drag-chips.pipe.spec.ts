import { DragChipsPipe } from './drag-chips.pipe';

describe('DragChipsPipe', () => {
  it('create an instance', () => {
    const pipe = new DragChipsPipe();
    expect(pipe).toBeTruthy();
  });
});
