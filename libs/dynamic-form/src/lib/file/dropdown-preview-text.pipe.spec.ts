import { DropdownPreviewTextPipe } from './dropdown-preview-text.pipe';

describe('DropdownPreviewTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownPreviewTextPipe();
    expect(pipe).toBeTruthy();
  });
});
