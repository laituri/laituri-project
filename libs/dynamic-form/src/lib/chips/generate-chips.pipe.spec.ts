import { GenerateChipsPipe } from './generate-chips.pipe';

describe('GenerateChipsPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateChipsPipe();
    expect(pipe).toBeTruthy();
  });
});
