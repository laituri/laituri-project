import { ValidationHintDirective } from './validation-hint.directive';

describe('ValidationHintDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationHintDirective();
    expect(directive).toBeTruthy();
  });
});
